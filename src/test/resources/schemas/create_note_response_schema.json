{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Create Note Response Schema",
  "description": "2.3 — JSON schema for POST /notes success response",
  "type": "object",
  "required": ["success", "status", "message", "data"],
  "properties": {
    "success": { "type": "boolean", "enum": [true] },
    "status":  { "type": "integer", "enum": [200] },
    "message": { "type": "string" },
    "data": {
      "type": "object",
      "required": ["id", "title", "description", "category", "completed", "user_id"],
      "properties": {
        "id":          { "type": "string" },
        "title":       { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "category":    { "type": "string", "enum": ["Home", "Work", "Personal"] },
        "completed":   { "type": "boolean" },
        "user_id":     { "type": "string" }
      },
      "additionalProperties": true
    }
  }
}
